image: radrydzewski/go:1.2
env:
  - GOPATH=/var/cache/drone
script:
  - go get .
  - go build
  - go test -v
services:
  - redis 
  - bradrydzewski/mongodb:2.4
notify:
  email:
    recipients:
      - ernado@ya.ru
